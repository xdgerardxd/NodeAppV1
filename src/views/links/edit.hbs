{{!-- Formulario Editar link --}}
<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto"> {{!--mx-auto margen automatico, centra la tarjeta dentro de la columna--}}
            <div class="card"> 
                <div class="card-body">
                    {{!-- Accion del boton, se debe crear una ruta donde se enviaran los datos, en links.js --}}
                    <form action="/links/edit/{{link.id}}" method="POST">
                        <div class="form-group">
                            {{!--Para mostrar los objetos del back hacia el front se usa value={{variable}}--}}
                            <input type="text" value="{{link.title}}" class="form-control" name="title" placeholder="Title" autofocus>
                            {{!-- TItulo de la casilla que mostrara en pantalla --}}
                        </div>
                        <div class="form-group">
                            <input type="url" value="{{link.url}}" class="form-control" name="url" placeholder="URL">
                        </div>
                        <div class="form-group">
                            <textarea class="form-control" name="description" rows="2"
                                placeholder="Description"> {{link.description}} </textarea> {{!--valor de text area va dentro de las etiquetas--}}
                        </div>
                        <div class="form-group">
                            <button class="btn btn-primary btn-block">Edit</button>
                            {{!--btn-success: color verder, btn-block: ocupe todo el ancho disponible--}}
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>